<script webc:type="js">
    let link = ""
    if (nextPost) {
         link = `
        <a id="next-link" href="${nextPost.url}">
            <span class="title">${nextPost.data.title}</span>
            <span id="arrow-forward" class="material-icons">arrow_forward_ios</span>
        </a>
       `
    }
    link

</script>